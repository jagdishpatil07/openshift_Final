apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-redis-scheduler
  namespace: production
spec:
  storageClassName: crc-csi-hostpath-provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
