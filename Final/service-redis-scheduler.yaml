apiVersion: v1
kind: Service
metadata:
  name: redis-scheduler
  namespace: production
spec:
  selector:
    app: redis-scheduler
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
