apiVersion: v1
kind: Service
metadata:
  name: postgres-new
  namespace: production
spec:
  selector:
    app: postgres-new
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
