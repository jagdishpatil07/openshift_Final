apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-data-store
  namespace: production
spec:
  storageClassName: crc-csi-hostpath-provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
