apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-user-defined-template
  namespace: production
spec:
  storageClassName: crc-csi-hostpath-provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
