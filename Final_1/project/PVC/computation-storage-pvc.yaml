apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-computation-storage
  namespace: production
spec:
  storageClassName: crc-csi-hostpath-provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
